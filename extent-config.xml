<?xml version="1.0" encoding="UTF-8"?>
<extentreports>
    <configuration>
        <!--report theme -->
        <!-- standard, dark -->
        <theme>standard</theme>
        <!-- document encoding -->
        <!-- defaults to UTF-8-->
        <encoding>UTF-8</encoding>
        <!-- protocol for script and stylesheets -->
        <!-- defaults to https -->
        <protocol>https</protocol>
        <!-- title of the document -->
        <documentTitle>Ground Labs</documentTitle>
        <!-- report name – displayed at top-nav -->
        <reportName>Ground Labs</reportName>
        <!-- report headline – displayed at top-nav, after reportHeadline -->
        <reportHeadline>Take Home Assignment Automation Test Report</reportHeadline>
        <!-- global date format override -->
        <!-- defaults to yyyy-MM-dd -->
        <dateFormat>yyyy-MM-dd</dateFormat>
        <!-- global time format override  -->
        <!-- defaults to HH:mm:ss -->
        <timeFormat>HH:mm:ss</timeFormat>
        <!-- default style override -->
        <theme></theme>
        <!-- custom javascript -->
        <scripts>
            <![CDATA[
 $(document).ready(function() {
 $(".logo-content").hide();
 $(".logo-container").html("Ground Labs");
 $('#parentNodes').prop("checked", true).click();
 $('#test-count-setting > div.modal-footer > ')[0].click();
 });
 ]]>
            <![CDATA[
        $(document).ready(function() {

        });
      ]]>
        </scripts>
        <!-- custom styles -->
        <styles>
            <![CDATA[
                .logo-container { padding-left: 10px; } .logo-container > img { float: left;height: 10%;margin-left: 130px;margin-top: 2px;width: auto; }
            ]]>
        </styles>
    </configuration>
</extentreports>